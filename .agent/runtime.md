# Runtime Recipe — contrail

_Generated by `~/.agent/bin/runtime-recipe`. Verify before relying on it for CI-critical work._

## Install

- `uv sync --locked`

## Common Commands

| Concern | Command |
|---|---|
| build | `uv build` |
| format_check | `uv run ruff format --check .` |
| lint | `uv run ruff check .` |
| test | `uv run pytest` |
| typecheck | `uv run mypy src tests` |

## Agent Notes

- Prefer the narrowest command that proves the current change.
- If this file disagrees with CI config, trust CI config and update this recipe.
- Do not invent commands that are not listed here or in repo docs; inspect tooling first.
- For stack-specific defaults, select the matching skill under `~/.agent/skills/` by path; this works across Claude, Codex, and Gemini.
