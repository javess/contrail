# Generated by ~/.agent/bin/runtime-recipe --write-commands. Verify before treating as CI policy.
[commands]
install          = "uv sync --locked"
lint             = "uv run ruff check ."
format_check     = "uv run ruff format --check ."
typecheck        = "uv run mypy src tests"
test_unit        = "uv run pytest"
test_integration = ""
docs_check       = ""
docs_build       = ""
build            = "uv build"
