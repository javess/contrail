# Malformed evidence corpus

These small, reviewable fixtures pin representative parser failures. Property
tests complement them with deterministic generated byte strings and nested JSON.
Keep fixtures minimal so a failure identifies one boundary condition.
